{
  "dependencies": {
    "bootstrap": "^5.2.2",
    "docs-server": "^1.6.2",
    "watch": "^1.0.2"
  },
  "scripts": {
    "start": "parcel serve src/index.html --public-url / --dist-dir dist"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.7.0"
  }
}
